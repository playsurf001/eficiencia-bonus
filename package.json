{
  "name": "corepro-eficiencia",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && npm run copy:public",
    "copy:public": "mkdir -p dist && cp -rn public/* dist/ 2>/dev/null || cp -r public/* dist/",
    "preview": "wrangler pages dev",
    "deploy": "npm run build && wrangler pages deploy dist --project-name corepro-eficiencia --branch main",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "hono": "^4.12.12",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "@hono/vite-build": "^1.2.0",
    "@hono/vite-dev-server": "^0.18.2",
    "vite": "^6.3.5",
    "wrangler": "^4.4.0"
  }
}
